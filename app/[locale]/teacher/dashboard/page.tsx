import { currentUser } from "@/lib/session";
import React from "react";

/**
 * Renders the teacher dashboard page.
 */
export default async function TeacherDashboard() {
  return <div>TeacherDashboard</div>;
}
